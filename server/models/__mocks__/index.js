const createNewCell = jest.fn();
const createNewComic = jest.fn();

const Cells = {
  createNewCell
}

const Comics = {
  createNewComic
}

const index = {
  Cells,
  Comics
}

module.exports = index;